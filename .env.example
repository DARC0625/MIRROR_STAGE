# MIRROR STAGE global configuration example

# === Backend (EGO) ===
MIRROR_STAGE_DB_URL=postgresql://mirror_stage:mirror_stage_password@localhost:5432/mirror_stage
MIRROR_STAGE_DB_SSL=false
MIRROR_STAGE_REDIS_URL=redis://localhost:6379

# Optional: disable ego self telemetry (default true)
# MIRROR_STAGE_EGO_MONITOR_ENABLED=true
# MIRROR_STAGE_EGO_MONITOR_INTERVAL_MS=5000

# Digital twin naming overrides
# EGO_HOSTNAME=ego-hub
# EGO_DISPLAY_NAME=MIRROR STAGE EGO
# EGO_PRIMARY_IP=10.0.0.100
# EGO_RACK=CoreRack

# === Reflector ===
# MIRROR_STAGE_REFLECTOR_CONFIG=/path/to/custom/config.json

